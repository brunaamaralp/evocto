/** Primeira página na aba Lançamentos (carregamento inicial mais rápido). */
export const FINANCE_TX_LIST_INITIAL_PAGE_SIZE = 60;

/** Lançamentos por requisição ao carregar mais / relatórios de movimentação, etc. */
export const FINANCE_TX_LIST_PAGE_SIZE = 200;

/** Teto aceito pela API ao pedir `?limit=` */
export const FINANCE_TX_LIST_MAX_PAGE_SIZE = 200;

/** Altura máxima das áreas de tabela com scroll no hub Financeiro (mais linhas visíveis) */
export const FINANCE_TABLE_MAX_HEIGHT = 'calc(100vh - 200px)';

/** Lista de inadimplência na aba Mensalidades */
export const FINANCE_INADIMPLENCIA_LIST_LIMIT = 60;

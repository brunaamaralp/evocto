import { useMemo } from 'react';

/**
 * Props estáveis para InboxContextPanel (lazy).
 */
export function useInboxContextPanelProps(params) {
  const {
    selectedPhone,
    selected,
    ticketChip,
    updateTicket,
    ticketUpdating,
    loadThread,
    setLeadPanel,
    canConfigureAgenteIa,
    openPromptSettings,
    academyId,
    conversationIdForFlags,
    toast,
    leadById,
    leadByPhone,
    normalizePhone,
    pickDisplayName,
    contactLabel,
    navigate,
    linkingLead,
    leadPanel,
    leadNameDraft,
    setLeadNameDraft,
    leadTypeDraft,
    setLeadTypeDraft,
    convertToLead,
    transferToDraft,
    setTransferToDraft,
    teamMembers,
    confirmTransferConversation,
    leadSearch,
    setLeadSearch,
    fetchLeads,
    leadsLoading,
    leadCandidates,
    linkLeadToConversation,
    studentCandidates,
    studentsLoading,
    fetchStudents,
    handleInboxConfirmTriage,
    handleInboxDismissTriage,
    handleOpenLinkStudent,
    handleInboxLinkStudentConfirm,
    activeContactLead,
    triageDismissed,
    restoreLeadTriage,
    pinnedMessages,
    setSelectedMsgKey,
    scrollToMsgKey,
    isMobile,
    setDetailsOpen,
    selectedPhoneFlags,
    membershipPrimaryLabel,
    terms,
  } = params;

  return useMemo(
    () => ({
      selectedPhone,
      selected,
      ticketChip,
      updateTicket,
      ticketUpdating,
      loadThread,
      setLeadPanel,
      canConfigureAgenteIa,
      openPromptSettings,
      academyId,
      conversationIdForFlags,
      toast,
      leadById,
      leadByPhone,
      normalizePhone,
      pickDisplayName,
      contactLabel,
      navigate,
      linkingLead,
      leadPanel,
      leadNameDraft,
      setLeadNameDraft,
      leadTypeDraft,
      setLeadTypeDraft,
      convertToLead,
      transferToDraft,
      setTransferToDraft,
      teamMembers,
      confirmTransferConversation,
      leadSearch,
      setLeadSearch,
      fetchLeads,
      leadsLoading,
      leadCandidates,
      linkLeadToConversation,
      studentCandidates,
      studentsLoading,
      fetchStudents,
      onConfirmTriage: handleInboxConfirmTriage,
      onDismissTriage: handleInboxDismissTriage,
      onOpenLinkStudent: handleOpenLinkStudent,
      onLinkStudentConfirm: handleInboxLinkStudentConfirm,
      triageBusy: linkingLead,
      activeContactLead,
      triageDismissed,
      onRestoreTriage: restoreLeadTriage,
      pinnedMessages,
      setSelectedMsgKey,
      scrollToMsgKey,
      isMobile,
      setDetailsOpen,
      selectedPhoneFlags,
      membershipPrimaryLabel,
      terms,
    }),
    [
      selectedPhone,
      selected,
      ticketChip,
      updateTicket,
      ticketUpdating,
      loadThread,
      setLeadPanel,
      canConfigureAgenteIa,
      openPromptSettings,
      academyId,
      conversationIdForFlags,
      toast,
      leadById,
      leadByPhone,
      normalizePhone,
      pickDisplayName,
      contactLabel,
      navigate,
      linkingLead,
      leadPanel,
      leadNameDraft,
      setLeadNameDraft,
      leadTypeDraft,
      setLeadTypeDraft,
      convertToLead,
      transferToDraft,
      setTransferToDraft,
      teamMembers,
      confirmTransferConversation,
      leadSearch,
      setLeadSearch,
      fetchLeads,
      leadsLoading,
      leadCandidates,
      linkLeadToConversation,
      studentCandidates,
      studentsLoading,
      fetchStudents,
      handleInboxConfirmTriage,
      handleInboxDismissTriage,
      handleOpenLinkStudent,
      handleInboxLinkStudentConfirm,
      activeContactLead,
      triageDismissed,
      restoreLeadTriage,
      pinnedMessages,
      setSelectedMsgKey,
      scrollToMsgKey,
      isMobile,
      setDetailsOpen,
      selectedPhoneFlags,
      membershipPrimaryLabel,
      terms,
    ]
  );
}
